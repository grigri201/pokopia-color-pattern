# Story 5.3: 验证全屏响应式、可访问性与 Open Design 样本一致性

Status: done

## Story

As a 维护者,
I want 全屏 overlay 有响应式、可访问性和样本验证,
so that Open Design 全屏体验不会在桌面、窄屏、移动端或后续构建中退化。

## Acceptance Criteria

1. Given 视口宽度大于 Open Design 桌面阈值, when 全屏 overlay 渲染, then 主体为左侧信息区、右侧 Pokemon 画像卡片的左右结构, and 左侧信息区包含上方身份区、下方主色/色板/图案区和偏好档案区。
2. Given 视口进入中窄屏或移动端, when 全屏 overlay 渲染, then 布局改为纵向滚动, and 信息区在上、Pokemon 画像卡片在下，信息区内部依次显示身份、主色/色板、偏好。
3. Given Open Design 指定样本 `No.180 利欧路`, when 全屏 overlay 渲染, then 中文界面、色板、偏好词换行和画像卡牌布局符合 Open Design 标记和视觉结构。
4. Given Open Design 指定样本 `Ditto`, when 全屏 overlay 渲染, then `偏好档案` 显示空状态, and 空状态不造成布局塌陷。
5. Given Open Design 指定样本 `Pikachu`, when 全屏 overlay 在移动视口渲染, then Pokemon 图像与标题尺度不重叠、不横向溢出, and 关闭按钮与语言切换按钮仍可点击和键盘访问。
6. Given production build 或 release verification 运行, when 全屏相关校验执行, then 验证全屏入口、打开、关闭、Escape、焦点返回、URL 不变和 `/assets/runtime/**` 图片路径, and 不引用 `/docs/pokopia_image_sources/**`。
7. Given 用户寻找壁纸导出能力, when 本轮 Epic 5 交付完成, then 页面不暴露未实现的导出按钮或假下载能力, and 文档保留“壁纸导出后续单独确认”的范围说明。

## Tasks / Subtasks

- [x] 添加响应式验证 (AC: 1, 2, 5)
  - [x] 覆盖桌面左右结构。
  - [x] 覆盖中窄屏和移动端纵向滚动结构。
  - [x] 检查标题、画像、偏好词和工具栏不重叠、不横向溢出。
- [x] 添加 Open Design 样本验证 (AC: 3, 4, 5)
  - [x] 覆盖 `No.180 利欧路`。
  - [x] 覆盖 `Ditto` 偏好词空状态。
  - [x] 覆盖 `Pikachu` 移动端图像与标题尺度。
- [x] 添加交互与可访问性验证 (AC: 6)
  - [x] 覆盖全屏入口、关闭按钮、Escape。
  - [x] 覆盖打开后的焦点移动和关闭后的焦点返回。
  - [x] 覆盖 URL/hash/history 不因全屏状态变化。
- [x] 添加 runtime boundary 验证 (AC: 6)
  - [x] 验证全屏 Pokemon image 使用 `/assets/runtime/**`。
  - [x] 验证全屏相关 HTML/JS/CSS 不引用 `/docs/pokopia_image_sources/**`。
- [x] 锁定壁纸导出范围 (AC: 7)
  - [x] 确认本轮没有可点击的下载/导出/生成壁纸假入口。
  - [x] 在文档或 story completion notes 中保留后续壁纸模板需单独确认的说明。

### Review Findings

- [x] [Review][Patch] Story 5.3 新增样本测试未显式断言 URL/hash/history 不变 [`tests/smoke/pokemon-page.spec.ts`]
- [x] [Review][Patch] 新增样本测试未断言关闭后的焦点返回 [`tests/smoke/pokemon-page.spec.ts`]
- [x] [Review][Patch] runtime boundary 只检查 overlay `src` 子集，未覆盖所有 fullscreen asset src 和 dist docs 引用 [`tests/smoke/pokemon-page.spec.ts`]
- [x] [Review][Patch] 无壁纸/导出/下载入口断言只看 visible text，漏掉 icon-only 控件 [`tests/smoke/pokemon-page.spec.ts`]
- [x] [Review][Patch] 几何断言 2px 容差过脆且未先验证元素尺寸 [`tests/smoke/pokemon-page.spec.ts`]
- [x] [Review][Patch] Riolu 样本未断言 sequence、`fullscreen-pattern` 和偏好词换行/溢出 [`tests/smoke/pokemon-page.spec.ts`]
- [x] [Review][Patch] Pikachu 移动端横向溢出检查应覆盖 fixed overlay 自身 [`tests/smoke/pokemon-page.spec.ts`]
- [x] [Review][Patch] 移动端工具按钮需验证点击/键盘激活可用性 [`tests/smoke/pokemon-page.spec.ts`]

## Dev Notes

- 本故事是 Epic 5 的 release-quality gate，不应新增未在 Open Design 中定义的新视觉模式。
- Open Design 断点参考：`1180px` 以上左右结构，`1180px` 以下纵向结构，`820px` 以下单列信息区。
- 如果 Playwright 可用，优先用 browser smoke 验证交互和视口；如果 build 环境不支持浏览器，至少保留可运行的 DOM/unit 验证，并说明 smoke 运行方式。
- 不要把壁纸导出当作本轮缺口补做。设计规格明确本轮不实现导出。

### Project Structure Notes

- 预期涉及：`tests/smoke/**` 或现有 smoke 测试、`src/styles.css`、`package.json` 验证脚本（如需要）。
- 不要让 `npm run build` 依赖不可用的浏览器安装；浏览器 smoke 可继续由 release verification 命令承载。

### References

- [Source: `_bmad-output/planning-artifacts/epics.md` - Epic 5 / Story 5.3]
- [Source: `_bmad-output/planning-artifacts/open-design-fullscreen-ui/design-spec.md` - 响应式、焦点、样本和壁纸后续约束]
- [Source: `_bmad-output/planning-artifacts/open-design-fullscreen-ui/index.html` - CSS breakpoints and Open Design DOM markers]
- [Source: `_bmad-output/project-context.md` - build/smoke/runtime asset validation rules]

## Dev Agent Record

### Agent Model Used

GPT-5 Codex

### Debug Log References

- 2026-05-17: 开始 Story 5.3 验证开发，基于现有 Playwright smoke suite 扩展全屏断点和样本覆盖。
- 2026-05-17: `npm run build` 通过；responsive samples smoke 和完整 smoke suite 通过。
- 2026-05-17: BMAD code review 返回 8 个测试覆盖 patch，已修复并复跑 build/full smoke。

### Completion Notes List

- 扩展 Playwright smoke，覆盖 Riolu 桌面左右结构和 Open Design 标记、1000px 中窄屏纵向结构、Ditto 偏好空状态、Pikachu 移动端无横向溢出/标题画像不重叠。
- 验证全屏 image 生产路径来自 `/assets/runtime/**`，并断言不使用 `/docs/pokopia_image_sources/**`。
- 验证全屏 overlay 中不暴露未实现的壁纸、导出、下载入口；壁纸模板仍需后续单独确认。
- 保留浏览器 smoke 在 `npm run verify:release` 下运行，不把 Playwright 加进 `npm run build`。
- Review 后补强 URL/hash/history、焦点返回、全量 fullscreen asset source、dist docs 引用、icon-only 导出入口、overlay 自身横向溢出、Riolu pattern/偏好词换行和移动端键盘/点击操作断言。

### Review Results

BMAD code review completed: Blind Hunter、Edge Case Hunter 和 Acceptance Auditor 共返回 8 个 patch finding；所有 patch 已修复并验证通过。

### File List

- `tests/smoke/pokemon-page.spec.ts`
- `_bmad-output/implementation-artifacts/sprint-status.yaml`
- `_bmad-output/implementation-artifacts/5-3-验证全屏响应式可访问性与样本一致性.md`

### Change Log

- 2026-05-17: Created from Open Design fullscreen UI.
- 2026-05-17: Added fullscreen responsive, sample, runtime-boundary, accessibility, and no-wallpaper smoke verification.
